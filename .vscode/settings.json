{
    // With dbaeumer.vscode-eslint v2.4.0, we need to enable this option explicitly.
    "eslint.experimental.useFlatConfig": true,
    // See https://github.com/microsoft/vscode-eslint/issues/1620
    "eslint.workingDirectories": ["integration_tests/"],

    "rust-analyzer.cargo.features": [
        // We want to be able to rollout this application to production at all
        // Thus we use _production_ as our default build feature set.
        "c_at_e_main/production"
    ]
}
